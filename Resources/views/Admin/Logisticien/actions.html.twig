<div class="btn-toolbar" role="toolbar" style="margin: 0;">
    <div class="btn-group">
         {% if is_granted('ROLE_LUNCH_ADMIN') %}

        <a href="{{ path('aclunch_admin_commande_index') }}" class="btn btn-primary" role="button"><i
                    class="glyphicon glyphicon-list"></i>
            Retour à la liste</a>
         {% endif %}
    </div>
</div>
<br/>