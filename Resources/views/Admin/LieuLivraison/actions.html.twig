<div class="btn-toolbar" role="toolbar" style="margin: 0;">
    <div class="btn-group">
        <a href="{{ path('aclunch_admin_lieulivraison_index') }}" class="btn btn-primary" role="button"><i
                    class="glyphicon glyphicon-list"></i>
            Retour à la liste</a>
    </div>
    <div class="btn-group">
        <a href="{{ path('aclunch_admin_lieulivraison_edit', { 'id': lieulivraison.id }) }}" class="btn btn-primary"
           role="button"><i
                    class="glyphicon glyphicon-edit"></i> Editer</a>
    </div>
</div>
<br/>